name: GradientMagnitudeImageFilter
template_code_filename: ImageFilter
template_test_filename: ImageFilter
doc: ''
number_of_inputs: 1
pixel_types: BasicPixelIDTypeList
output_pixel_type: float
members:
- name: UseImageSpacing
  type: bool
  default: 'true'
  briefdescriptionSet: ''
  detaileddescriptionSet: >-
    Set/Get whether or not the filter will use the spacing of the input image in the computation of the derivatives. Use On
    to compute the gradient in physical space; use Off to ignore image spacing and to compute the gradient in isotropic voxel
    space. Default is On.
  briefdescriptionGet: ''
  detaileddescriptionGet: >-
    Set/Get whether or not the filter will use the spacing of the input image in the computation of the derivatives. Use On
    to compute the gradient in physical space; use Off to ignore image spacing and to compute the gradient in isotropic voxel
    space. Default is On.
tests:
- tag: default
  description: Simply run with default settings
  settings: []
  tolerance: 1.0e-05
  inputs:
  - Input/RA-Float.nrrd
briefdescription: Computes the gradient magnitude of an image region at each pixel.
detaileddescription: >-
  \see Image



  \see Neighborhood



  \see NeighborhoodOperator



  \see NeighborhoodIterator
itk_module: ITKImageGradient
itk_group: ImageGradient
in_place: false
